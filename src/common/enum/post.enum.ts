
export enum Availability_Enum {
    public = "public",
    friends = "friends",
    only_me = "only_me"
}

export enum Allow_Comment_Enum {
    allow = "allow",
    deny = "deny"
    
}

export enum On_Model_Enum {
    Post = "Post",
    Comment = "Comment"
}